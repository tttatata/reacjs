'use strict';

var _isEqual = require('./is-equal');

var _isEqual2 = _interopRequireDefault(_isEqual);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('utils', function () {
  describe('isEqual()', function () {

    test('two empty objects', function () {
      var res = (0, _isEqual2.default)({}, {});
      expect(res).toEqual(true);
    });

    test('one empty and one non-empty obj', function () {
      var res = (0, _isEqual2.default)({}, { a: 2 });
      expect(res).toEqual(false);
    });

    test('two objects with diff num of keys', function () {
      var res = (0, _isEqual2.default)({ a: 2, b: 1 }, { a: 2 });
      expect(res).toEqual(false);
    });

    test('two objects with same keys/props in diff order', function () {
      var res = (0, _isEqual2.default)({ a: 2, b: 1 }, { b: 1, a: 2 });
      expect(res).toEqual(true);
    });

    test('two objects with same keys but diff props', function () {
      var res = (0, _isEqual2.default)({ a: 10, b: 11 }, { a: 12, b: 13 });
      expect(res).toEqual(false);
    });
  });
}); // local imports